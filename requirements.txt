# Core dependencies
streamlit>=1.28.0
nbformat>=5.7.0
nbconvert>=7.0.0
jinja2>=3.0.0

# Syntax highlighting and markdown
pygments>=2.15.0
markdown>=3.4.0

# PDF conversion backends (optional - at least one recommended)
# WeasyPrint provides higher quality output but requires system dependencies
weasyprint>=60.0

# xhtml2pdf is a pure Python alternative (fallback)
xhtml2pdf>=0.2.11

# HTTP requests for URL/Gist support
requests>=2.28.0
